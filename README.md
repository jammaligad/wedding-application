# Mobile Wedding Application as the Event Invitation

## Details TBA

## Routable Tab URLs (Different HTMLs) structure:

```
.views
  .toolbar.tabbar.toolbar-bottom-md
  .view.view-1 (usually view-main)
    .page
      .toolbar (the tabs routable)
```
